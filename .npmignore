# Keep node_modules for bundled dependencies (needed for global install)
# node_modules/ is INCLUDED via bundledDependencies

# Exclude client node_modules (not needed, only build is needed)
client/node_modules/
package-lock.json
client/package-lock.json

# Development files
.env
.env.local
.env.*.local
!.env.example

# Client source files (we only need the build)
client/src/
client/public/
client/.gitignore
client/README.md
client/.env
client/.env.*

# Database files (contain sensitive data)
server/data/*.db
server/data/*.db-journal
*.db
*.db-journal

# Generated certificates
server/certs/*.pem
server/certs/*.crt
server/certs/*.key

# SSH keys (NEVER include)
*.pem
*.key
id_rsa*
id_ed25519*
id_dsa*
id_ecdsa*
*.ppk
known_hosts
authorized_keys

# IDE
.vscode/
.idea/
.claude/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
._*

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Testing
coverage/
.nyc_output/
__tests__/
*.test.js
*.spec.js

# Temporary files
*.tmp
*.temp
*.tgz
*.tar.gz
*.bak
*.backup

# Git
.git/
.gitignore
.gitattributes

# CI/CD
.github/
.gitlab/
.travis.yml
.circleci/

# Documentation (keep only README.md)
*.md
!README.md
